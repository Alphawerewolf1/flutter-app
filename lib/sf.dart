import 'package:flutter/material.dart';

class SfPage extends StatelessWidget {
  const SfPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('SF Page Placeholder', style: TextStyle(fontSize: 24)),
    );
  }
}
